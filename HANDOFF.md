# Handoff Document

**Date:** 2026-08-05
**Branch:** `main`
**Status:** Adding mobile map scroll cue below elevation profile.

> **All agents:** read `AGENTS.md` ("Mandatory Agent Workflow") before making any change.

## Current production snapshot

- Frontend: `de8fabc` (clean redeploy of mobile Resources tap fix). Hard-refresh and compare the footer hash.
- AC100 training overlaps backfilled in DB (all 6 routes).
- Repository: `main` dirty — map mobile scroll affordance in progress.

## In progress

- Map & Aid Stations (mobile): cue below elevation that route stats / aid list continue; slightly shorter map so the cue fits on-screen.

## Just finished

- Clean redeploy so footer is `de8fabc` (not `afcec73-dirty`).
- Mobile Resources tap fix; ability prediction hidden; amenity icons aligned.
